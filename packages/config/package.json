{
  "name": "@immoradar/config",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run --root ../.. --passWithNoTests packages/config/src/**/*.test.ts",
    "test:unit": "vitest run --root ../.. --passWithNoTests packages/config/src/**/*.test.ts",
    "clean": "rm -rf dist"
  }
}
